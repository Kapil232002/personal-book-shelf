services:
  - type: web
    name: BookNest
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: |
      python manage.py migrate
      python manage.py collectstatic --noinput
      python manage.py create_initial_superuser
      gunicorn book_manager.wsgi:application
